# Personal Notebook AI — dependencias de ejecución
# Rangos con techo de major para builds reproducibles sin romper instalaciones nuevas.

# --- Backend web ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
pydantic>=2.7,<3.0
python-multipart>=0.0.9,<1.0     # UploadFile (restore de backups)

# --- Audio / señal ---
numpy>=1.26,<3.0
PyAudioWPatch>=0.2.12.6 ; sys_platform == "win32"   # loopback WASAPI (Windows)
imageio-ffmpeg>=0.5,<1.0                            # binario ffmpeg embebido
edge-tts>=6.1,<8.0                                  # TTS del podcast (gratis, sin llave)

# --- Gadget flotante ---
pywebview>=5.0,<7.0 ; sys_platform == "win32"

# --- Desarrollo / pruebas ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
